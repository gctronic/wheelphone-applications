<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppBaseTheme" parent="android:Theme.Holo.Light">
    </style>

    <style name="FullscreenTheme" parent="AppBaseTheme">
        <item name="android:actionBarStyle">@style/FullscreenActionBarStyle</item>
        <item name="android:windowActionBarOverlay">true</item> <!-- actionbar (app title) as overlay on top of the app -->
        <item name="android:windowFullscreen">true</item> <!-- Hide clock bar -->
    </style>

    <style name="FullscreenActionBarStyle" parent="android:Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/white_overlay</item>
    </style>

</resources>